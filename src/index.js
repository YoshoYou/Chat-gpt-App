import React from 'react';
import ReactDom from 'react-dom';


import App from './App';

 ReactDom.render(<App></App>, document.getElementById( 'root' ) ); //the app compnent will be render inside the div whit ip of root 